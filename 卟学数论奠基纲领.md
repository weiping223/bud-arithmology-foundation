# 卟学数论奠基纲领：以集合为胚、以后继为迭代、以投影复位经典数

## 摘要：点胚不必破坏经典算术，但数系的逐层嵌入仍是未完成的模型论承诺

本报告以用户上传的《辩证逻辑下的新数学》为第一手材料，并在其三条原始公理、投影接口与“革命＋延拓”策略中，插入一个集合论生成内核。核心结论是：**点胚不是经典空集，而是带内部拓扑或两叶结构的原始集数；它可以直接作为集数迭代的“第零级对象”，再通过并集式后继、等价类、笛卡尔积及完备化逐级生成自然数、整数、有理数、实数与复数。**

这一纲领成立的关键在于严格区分三类相等：胚内拓扑等同 `$0 \simeq_\vartheta 1$`、点胚代数中的结构性等同，以及经典集合论意义上的外延相等 `$=$`。原论文已规定内部等式 `$0=1$` 不能穿越投影 `$\pi$`，投影又只在经典有定义处复位。因此，“胚内革命”与“外部经典算术”的兼容不是通过全局承认 `$0=1$` 实现，而是通过限定等式有效范围、隔离内部结构和控制投影输出实现。若把胚内等同直接提升为经典外延相等，经典逻辑仍会崩溃。

集数观还自然导出一类算术不变量：**生成深度**。粗略地说，一个 von Neumann 集数的深度是其套括号或递归生成次数。它不同于基数与序数，不能在投影 `$ \pi $` 下无损保持；但整数、有理数与实数构造中的可数性、可构造性及代表元复杂度，都可在深度概念下重新表述。由此得到的不是素数的现成判别法，而是一片等待立法的前域。

本报告**不定义素数**，只在整数与可构造性的接口处说明素数理论需要补充哪些结构、等式与不动点条件。现有材料同样不足以证明原体系相对 ZF 的一致模型：将 `$ \vartheta $` 实现为 ZF 中的具体两叶对象，并把内部等同映射为明确的等价关系，仍是额外承诺。因此，相对一致性应表述为：**若能够成功构造胚层模型，则卟学可以保守地扩展经典算术；模型是否存在，不能仅由宣言替代。**

## 一、核心洞见决定体系结构：先有分层相等，才有集合论版本的“革命＋延拓”

**本研究不是把原论文改写为标准集合论，而是在其纤维丛式世界观中增设一个严格底座。** 点胚承担数系生成的原始结构，投影承担向经典数的复位，二者之间再通过分层语言隔离胚内等同与外部相等。只有这一结构完整，才有资格讨论自然数的递归生成与经典算术的兼容。

附件明确采用“辩证逻辑下的新数学”立场：公理 I 置放点胚 `$ \vartheta =(0,1) $` 与内部 `$0=1$`；公理 II 置放 `$1/0=\infty$`；公理 III 把直线闭合成射影圆；投影接口 `$ \pi $` 是遗忘 `#` 分量的“遗忘函子”，在经典有定义处满足 `$ \pi\circ B=\mathrm{id} $`，在经典无定义处不作用。原文又称 `$ \vartheta $` 为莫比乌斯环两面、无穷小本体、补断生成元；`$0=1$` 被声明为环内拓扑等同，而非外部任意上下文中的替换规则。[附件 §2、§2.1]

用户所选择的“数是集数”路线，要求把数进一步解释为集合论对象。本报告采用以下总约定：

> **数是一个带类型的集合或集合构造。** 一个“集数”`$x$`由类型标签 `$\mathrm{ty}(x)$`、承载集合 `$|x|$`、生成深度 `$\deg(x)$` 与经典投影 `$\pi(x)$` 组成；只有相同类型且具有合适相等态射的对象才能比较或运算。

由此可以避免把“自然数”“整数”“分数”“实数”粗暴压入同一个集合。von Neumann 的经典方案令

`$$0=\varnothing,\qquad S(x)=x\cup\{x\},$$$`

从而

`$$1=\{0\},\quad 2=\{0,1\},\quad n=\{0,1,\dots,n-1\}.$$$`

这一构造及其满足 Peano 公理的说明，见北京大学讲义对 Peano 系统与 von Neumann 后继的整理；南京大学离散数学讲义则给出无穷公理与 `$\mathbb N$` 的归纳构造。[2][3] 本报告的差异在于：在经典 von Neumann 链之前，另设一个不由空集替代、却可映射为经典初始结构的胚层对象 `$ \vartheta $`。

| 原文术语 | 本报告采用的严格角色 | 分类 | 与经典集合论的关系 |
|---|---|---|---|
| 点胚 `$ \vartheta=(0,1) $` | 带内部两叶/莫比乌斯结构的原始集数 | 原体系已有 | 不是 `$ \varnothing $`；需要新模型承诺 |
| 内部 `$0=1$` | `$0\simeq_\vartheta 1$`，即胚内拓扑等同 | 原体系已有 | 不是全局外延相等 |
| 投影 `$ \pi $` | 遗忘内部纤维，只保留经典读数 | 原体系已有 | 一般不是同态 |
| 封装、受控泄漏 | 限制胚内等式与 `#` 分量进入经典层 | 原体系已有 | 本报告给出类型论解释 |
| 残留阶数 | 奇点补断中尚未抵消的结构指标 | 原体系已有 | 不作为自然数深度定义 |
| 集数、深度、`$S$` | 带类型集合与递归生成 | **本研究新推** | 兼容 von Neumann 序数 |
| 胚层 ZF 模型、解释函数 | 把原体系实现为集合论结构 | **本研究假设** | 一致性尚待模型构造 |

### 1.1 革命与延拓只能在分层相等中同时成立

**原体系的“革命＋延拓”不是一句并列口号，而是一组关于等式作用域的结构性条件。** 若 `$0=1$` 在经典层处处有效，则从 `$0=0$` 得到 `$0=1$`、再传递得到任意对象相等，经典算术立即崩溃。因此，“经典算术 `$0\ne 1$` 保持不变”与“胚内 `$0=1$`”不可能在同一等价关系下同时成立。

附件自己提供了逃生通道：投影条款规定 `$ \pi(0=1) $` 无定义，仅输出经典分量的 `$ \pi(\vartheta)=(0,1) $`；在经典无定义处，`$ \pi $` 不作用。这相当于规定“等式不能自由穿越投影”。[附件 §2.1] 本报告据此采用如下基本语言：

- `$a=b$`：同一胚层、同一类型中的严格相等；
- `$a\simeq_\vartheta b$`：仅在点胚内部有效的拓扑等同；
- `$ \pi(a)=\pi(b) $`：经典投影相等。

严格相等与投影相等都允许替换；胚内等同 `$ \simeq_\vartheta $` 只在其适用纤维内替换。除非存在专门的提升或投影条件，不得由 `$a\simeq_\vartheta b$` 推出 `$a=b$`，更不得推出 `$ \pi(a)=\pi(b) $`。换言之，附件中的“内部 `$0=1$`”应读成：

`$$0_\vartheta\simeq_\vartheta 1_\vartheta,$$$`

而不是：

`$$0_\vartheta=1_\vartheta.$$$`

这一改写是相容性边界，不是文字游戏。只有禁止胚内等同进入经典替换语境，才有资格继续讨论经典算术的完整复位。


*图 1：从点胚到经典 von Neumann 集数的逐级生成。数据来源：附件《辩证逻辑下的新数学》§2、§4；von Neumann 后继定义 [2]。*

## 二、“数是集数”必须由外延、生成与投影三项条件共同定义

**一个对象被称为数，不只是因为它由集合构成，还因为它带有可重复识别的生成方式与可复位到经典层的投影。** 仅规定“数是集合”不足以建立数论；必须同时固定承载集合、生成深度、运算与投影接口。

### 2.1 定义：集数

**定义 2.1（集数）.** 固定一个类型宇宙 `$ \mathcal T $`。一个**集数**是四元组

`$$x=(|x|,\mathrm{ty}(x),\deg(x),\pi_x),$$$`

其中：

1. `$|x|$` 是 ZF 中的集合；
2. `$\mathrm{ty}(x)\in\mathcal T$` 标示对象所属的数层；
3. `$\deg(x)\in\mathbb N\cup\{\omega\}$` 是生成深度；
4. `$\pi_x$` 是从 `$x$` 的经典可读部分到某个经典结构 `$S$` 的偏映射。

当 `$ \pi_x $` 有定义时，记

`$$\underline x=\pi_x(x).$$$`

并称 `$ \underline x $` 为 `$x$` 的**经典像**。

“集数”中的“set”首先是集合论意义上的 set；除特别说明外，本报告默认在 ZF 或 ZFC 的元层中构造承载集合。原体系的胚并不因此被还原为普通集合：其特殊性被保存在类型、内部等同与投影接口中。

空集数 `$0_V$` 不是原胚，而是承担经典零的 von Neumann 对象：

`$$0_V=\varnothing.$$$`

其经典像为

`$$\underline{0_V}=0.$$$`

这一点不可省略。若令 `$ \vartheta=\varnothing $`，则由空集外延立即得到 `$0=1$` 在经典层也成立，封装条款将被绕过。因此，集数立场必须把 `$ \vartheta $` 与 `$0_V$` 分开。

### 2.2 公理：原始胚及其投影边界

**公理 A（点胚原始性，原体系已有＋本研究包装）.** 存在一个原始类型对象

`$$\vartheta=(\Theta,\simeq_\vartheta,\pi_\vartheta),$$$`

满足：

1. `$\Theta$` 是一个带拓扑或两叶结构的集合；
2. 存在胚内标记 `$0_\vartheta,1_\vartheta\in\Theta$`，且 `$0_\vartheta\simeq_\vartheta 1_\vartheta$`；
3. `$ \pi_\vartheta $` 定义在经典可读部分上，并且 `$ \pi_\vartheta(\vartheta)=(0,1) $`；
4. 胚内等式 `$0_\vartheta\simeq_\vartheta 1_\vartheta$` 不在 `$ \pi $` 的作用域内；
5. 除复位值外，内部 `$ \vartheta $`-信息投影归零。

第五项可写成

`$$\pi(\vartheta\star C)=\pi(\vartheta)\cdot 0+\text{经典余项},$$$`

但具体记号仍需独立选择公理。原体系把它称为**受控泄漏**：新内容原则上能够影响外部，却只能以受控残差出现。附件进一步声称残留由“残留阶数定理”锁定；对于 `$1/x$` 型的局部奇点，附件给出各阶补断值。[附件 §5.5] 但这一微积分规则不能自动成为集合论模型的定理。**受控泄漏的一般代数形式仍属待解决接口。**

A3–A5 可以总结为“点胚是原始集数”：

`$$\deg(\vartheta)=0,$$$`

`$$\mathrm{ty}(\vartheta)=\text{胚},$$$`

`$$| \vartheta |=\Theta.$$$`

深度 `$0$` 表示它不通过后继算子由更低集数构造，而非表示其集合论复杂度为空。空集数 `$0_V=\varnothing$` 同样可赋予深度 `$1$`，因为它是生成链的第一个严格集合构造。


*图 2：胚层、投影与经典层的分层。数据来源：附件《辩证逻辑下的新数学》§2.1，条款 (ii)、(iii)、(v)。*

### 2.3 内部寂静是替换控制，而不是“内部没有内容”

**定义 2.2（内部寂静）.** 对胚层语句 `$ \varphi $`，记 `$ \llbracket \varphi \rrbracket _\vartheta $` 为其在 `$ \Theta $` 中的内部解释。若 `$ \pi $` 未定义于该语句，则称它是**寂静的**；若对经典变量替换封闭，则称它是**可投影的**。

原体系称“内部寂静”为 `$ \Delta L=0 $`，并把它视为补断态不增加独立动力学的条件。[附件 §5] 在本报告中，它首先是一个逻辑约束：胚层可以容纳丰富推导，但任何导出等式必须先通过封装边界，才能进入经典层。

**引理 2.3（无偷渡原则）.** 若 `$a,b$` 是经典可读集数，且由使用 `$ \simeq_\vartheta $` 的推导得到 `$a=b$`，那么该项推导在投影边界处不合法，除非每一步均已被证明保持 `$ \pi $`。

> **证明.** 设存在合法推导。若某一步直接由 `$0_\vartheta\simeq_\vartheta 1_\vartheta$` 替换经典上下文中的不同对象，则 `$ \pi $` 条款 (ii) 被违反。因此，任何合法推导都必须经由类型允许的态射，而不能经由裸等同。□

原体系提出“无偷渡”方向，但未给出形式证明。引理 2.3 只是把该方向重写成类型论防火墙，距离完整形式化仍有明显距离。

## 三、点胚必须作为生成起点，而不能与空集或经典零混同

**点胚是“第零个集数”，不是经典链的第零项。** 它可以生成后继链并产生经典读数，但其内部结构不能被空集替代；同时，经典自然数链也不能反过来吸收胚内等同。

### 3.1 点胚与空集承担不同结构角色

**定义 3.1（零级集数与第零集数）.** 令 `$ \vartheta $` 为**零级集数**，它是所有迭代的真正起点；令

`$$0_V=\varnothing$$$`

为**第零 von Neumann 集数**，即经典自然数链的起点。

二者之间的关系是

`$$\pi(\vartheta)=(0,1),$$$`

而不是

`$$ \vartheta =\varnothing.$$$`

原体系没有把 `$ \vartheta $` 定义为空集，反而把它描述为两叶、莫比乌斯结构或无穷小本体。[附件 §2] 因此，承认原体系的革命性，恰恰要求**拒绝**把 `$ \vartheta $` 与 `$ \varnothing $` 直接等同。

| 对象 | 承载结构 | 深度 | 经典投影 | 相等性质 |
|---|---|---:|---|---|
| `$ \vartheta $` | 胚、两叶/莫比乌斯结构 | 0 | `$(0,1)$` | `$0_\vartheta\simeq_\vartheta 1_\vartheta$` |
| `$0_V=\varnothing$` | 空集 | 1 | `$0$` | `$ \forall x(x\notin\varnothing) $` |
| `$1_V=\{0_V\}$` | 单点集 | 2 | `$1$` | `$0_V\in1_V$` |
| `$2_V=\{0_V,1_V\}$` | 双点集 | 3 | `$2$` | `$1_V\in2_V$` |

胚与空集可分，不等于二者完全没有关系。可以把 `$ \vartheta $` 的“可见经典截面”解释为由 `$0_V$` 与 `$1_V$` 形成的离散纤维，再把内部扭转解释为交换两叶的附加结构。这正对应原体系所说的“莫比乌斯环两面投影为 `$0$` 与 `$1$`”。不过，这种解释只是**模型构造的接口假设**，不是从三条公理推出的唯一结果。

### 3.2 原体系的整数生成必须与集合论生成并行

**原体系已有结构不能直接充当 Peano 后继。** 附件 §4 把整数描述为莫比乌斯半圈累积，令 `$S$` 为前进半圈，并写出

`$$S^2=\mathrm{id}.$$$`

若把 `$S$` 直接理解为经典后继，就会得到周期为 `$2$` 的链

`$$p_0,p_1,p_0,p_1,\dots,$$$`

而不能生成 `$0,1,2,3,\dots$`。因此，附件中的“加 `$1$`”与本研究中的集合论后继必须分别处理：前者是莫比乌斯底圆的扭转采样，后者才负责生成 von Neumann 序数链。[附件 §4]

**定义 3.2（双轨后继）.** 固定点胚 `$ \vartheta $`。

1. **扭转后继** `$T$`：沿莫比乌斯结构前进半圈。原体系有 `$T^2\approx_\vartheta\mathrm{id}$`。它负责附件中的极性、扭转和 `$0/1$` 交换。
2. **集数后继** `$S$`：对可投影的集数，定义为
   `$$S(x)=(x\cup\{x\},\mathrm{ty}(x)+1,\deg(x)+1,\pi_{S(x)}),$$$`

   其中 `$ \pi_{S(x)} $` 由经典后继 `$ \underline{S(x)}=\underline x+1 $` 唯一指定。

两个后继不等同：

`$$T\neq S.$$$`

扭转后继把 `$0$` 与 `$1$` 的两叶结构互换；集数后继则把已有集合并入自身，再增加一项。二者可以耦合，却不能互相替代。

**命题 3.3（投影兼容）.** 若 `$x$` 是经典可读集数，则

`$$\pi(S(x))=\pi(x)+1.$$$`

> **证明.** 由 `$S$` 的定义，其经典读数按 von Neumann 后继复位。对经典自然数，`$x\cup\{x\}$` 的后继恰好对应 `$ \underline x+1 $`。□

关键在于：这里发生复位的是**读数** `$ \underline x $`，不是裸胚。胚内虽有 `$0\simeq_\vartheta1$`，投影也只能输出经典对 `$(0,1)$`，不能输出全局等式 `$0=1$`。

## 四、自然数链必须由独立起点与迭代公理生成，半圈闭合不能替代归纳

**自然数的单向无穷性只能由归纳类型或无穷公理保证，不能通过莫比乌斯闭合得到。** 点胚可以提供初始结构并映射为 `$(0,1)$`，却不能既是自然数 `$0$` 又是自然数 `$1$`。

### 4.1 自然数定义：从胚经投影读数，再以空集开始迭代

**定义 4.1（自然数集数）.** 定义

`$$0_N:=0_V=\varnothing,$$$`

并对已构造的集数 `$n$``$$n+1:=S(n)=n\cup\{n\}.$$$`

令 `$ \mathbb N_{\mathrm{set}} $` 为同时满足以下条件的最小类：

- `$0_N\in\mathbb N_{\mathrm{set}}$`；
- 若 `$n\in\mathbb N_{\mathrm{set}}$`，则 `$S(n)\in\mathbb N_{\mathrm{set}}$`；
- 只有由有限次 `$S$` 作用于 `$0_N$` 所得的对象属于该类。

这是 von Neumann 的经典定义。权威教材性材料明确采用 `$0=\varnothing$`、`$S(x)=x\cup\{x\}$`，并把 `$ \mathbb N $` 定义为归纳集的交集。[3] 北京大学讲义也给出 Peano 五公理及归纳法原理。[2]

本研究作出的实质性选择是：

`$$0_N\neq\vartheta,$$$`

但允许存在结构映射

`$$p:\vartheta\longrightarrow (0_N,1_N).$$$`

这一选择解决了原体系的表面张力。`$ \vartheta $` 不是自然数 `$0$` 本身，而是将 `$0_N$` 与 `$1_N$` 并置为纤维、并在内部等同两叶的结构。经典自然数链仍从 `$ \varnothing $` 开始；胚则位于其下方，作为投影前结构。

### 4.2 定理：经典 Peano 结构在投影下复原

**定理 4.2（投影复位自然数）.** 在经典可读子集 `$D_{\mathrm{cl}}$` 上，定义

`$$\underline n=\pi(n).$$$`

则 `$ \underline n $` 构成经典 Peano 系统：

1. `$ \underline 0=0 $`；
2. `$ \underline{S(n)}=\underline n+1 $`；
3. `$ \underline n=0 $` 当且仅当 `$n=0_N$`；
4. `$ \underline m=\underline n $` 蕴含 `$m=n$`；
5. 对任意子集 `$A\subseteq\mathbb N_{\mathrm{set}}$$0_N\in A$` 且 `$A$` 对 `$S$` 封闭，则 `$A=\mathbb N_{\mathrm{set}}$`。

> **证明.** 前四项由 von Neumann 构造保证：自然数按集合属于关系严格排列，`$m\in n$` 与 `$m<n$` 一一对应，北京大学讲义亦明确列出严格全序与三歧性。[2]
>
> 第五项由定义 4.1 的最小性直接得到：若 `$A$` 包含初始对象并对后继封闭，则任何由有限次后继生成的对象都属于 `$A$`。□

这里的归纳公理不是附件中莫比乌斯底圆的周期，而是 von Neumann 链的最小归纳闭包。二者可以共享“从原始结构出发逐步生成”的直觉，却不能在逻辑上互相替代。

**命题 4.3（胚不是 `$0=1$` 的语义后门）.** 假设 `$ \pi $` 把严格集数等同恢复为经典相等。若同时存在映射 `$f$` 使得

`$$f(0_\vartheta)=0_N,\qquad f(1_\vartheta)=1_N,$$$`

并且胚内等同满足

`$$0_\vartheta\simeq_\vartheta1_\vartheta,$$$`

那么 `$f$` 只能被解释为纤维内的结构态射，**不能**解释成保持外延相等的单射。否则将有

`$$0_N=1_N,$$$`

经典 Peano 结构随之崩溃。

> **证明.** 若 `$f$` 保持严格相等，则由 `$0_\vartheta\simeq_\vartheta1_\vartheta$` 推得 `$f(0_\vartheta)=f(1_\vartheta)$`，即 `$0_N=1_N$`。这与 `$S(n)=0_N$` 无解及后继单射矛盾。□

因此，点胚的“两面其实是一面”只能理解为：**沿特定内部路径回到同一结构**，并非集合论意义上的同一元素。若不维持这一区分，封装条款就只是口头限制，无法阻止 `$0=1$` 外泄。

### 4.3 内部闭合与外部单向性构成互补，而非冲突

**定义 4.4（生成链）.** 定义

`$$a_0=\vartheta,$$$`

`$$a_{n+1}=S(a_n),$$$`

并把 `$a_n$` 称为**胚迭代集数**。

若把 `$ \vartheta $` 原样代入并集后继，则由于 `$ \vartheta $` 不是普通集合，表达式 `$ \vartheta\cup\{\vartheta\} $` 只有经过适当的承载集解释才有意义。因此，本报告不采用“`$0=1$`，故 `$S(\vartheta)=\vartheta$`”这一错误路线。正确路线是：

- 用 `$ \vartheta $` 生成或标记自然数投影对；
- 用 `$0_N,1_N,2_N,\dots$` 作为外延确定的集合；
- 再要求投影与后继满足自然相容关系。

| 层级 | 集数对象 | 经典投影 | 深度 | 所需经典基础 | 卟学新增 |
|---|---|---:|---:|---|---|
| `$0$` | `$ \vartheta $` | `$(0,1)$` | 0 | 空集合数与胚结构 | 胚拓扑、内部等同 |
| `$1$` | `$0_N=\varnothing$` | 0 | 1 | 空集、对集 | 投影识别约定 |
| `$2$` | `$1_N=\{0_N\}$` | 1 | 2 | 单点集 | — |
| `$3$` | `$2_N=\{0_N,1_N\}$` | 2 | 3 | 并集、归纳 | — |
| `$ \omega $` | `$ \mathbb N_{\mathrm{set}} $` | `$ \omega $` | `$ \omega $` | 无穷公理 | 投影相容 |
| 整数层 | `$ \mathbb Z_{\mathrm{set}} $` | 整数环 | 可数 | 差集、等价类 | 补断负向扭转 |
| 有理数层 | `$ \mathbb Q_{\mathrm{set}} $` | 有序域 | 可数 | 笛卡尔积、商 | 四同态截面接口 |
| 实数层 | `$ \mathbb R_{\mathrm{set}} $` | 完备有序域 | 不可数 | ZF＋完备性 | 完备性为独立假设 |
| 复数层 | `$ \mathbb C_{\mathrm{set}} $` | 代数闭域 | 不可数 | `$ \mathbb R^2 $`、多项式 | 双层莫比乌斯 |

原始层级数据可下载：[层级数据 CSV](/data/workspace/dr:卟学数论/层级数据.csv)。

## 五、加法与乘法来自集合构造，序则来自属于关系，但投影不能成为同态

**集数算术不是把经典运算机械搬到胚层，而是由并集、笛卡尔积、函数空间与商构造诱导，再经投影复位。** 只要胚内存在非平凡合并或坍缩，投影一般就无法同时保持加法与乘法。

### 5.1 加法由不交并诱导，乘法由笛卡尔积诱导

**定义 5.1（集数运算）.** 对同类型有限集数 `$x,y$`，定义：

- `$x+y$`：先取不交复制 `$x',y'$`，再取并集 `$x'\cup y'$`；深度为
  `$$\max(\deg(x),\deg(y))+1;$$$`
- `$x\cdot y$`：取笛卡尔积 `$x\times y$`，并按标准编码映射为集合；深度为
  `$$\deg(x)+\deg(y)+1;$$$`
- `$x\le y$`：当且仅当 `$ |x|\subseteq|y| $` 时成立。

对于 von Neumann 自然数，最后一项退化为经典属于序：

`$$m<n\iff m\in n.$$$`

这正是北京大学讲义给出的序关系。[2] 但对一般集数，`$|x|\subseteq|y|$` 只是本研究采用的结构序，并非任意数系的普遍定义。

### 5.2 投影兼容与同态保持不能混为一谈

**定理 5.2（经典算术复位）.** 在经典可读、且投影有定义的子集上：

`$$\pi(x+y)=\pi(x)+\pi(y),\qquad \pi(x\cdot y)=\pi(x)\cdot\pi(y).$$$`

> **证明.** 这是一项**后续承诺**，而非自动定理。要使其成立，必须规定：
>
> 1. 胚层携带足够的经典表示；
> 2. 并集、不交并与笛卡尔积按经典规则编码；
> 3. 投影忘记的 `#` 信息在这些运算中不产生非零经典残差。
>
> 在 von Neumann 子链上，由于对象与运算均按经典集合论定义，加法与乘法可递归验证。对带有非平凡 `#` 分量的对象，该命题需要逐层证明；现有三条公理尚不足以推出其普遍性。□

原文明确称 `$ \pi $` 是遗忘函子而非同态。[附件 §2.1] 因而不能笼统断言“所有运算均复位”。更准确地说：

- `$ \pi $` 在经典定义域上**恢复经典对象**；
- 对特定“足够纯”的运算，可进一步证明**投影同态性质**；
- 对补断、坍缩、扭转等胚层运算，投影可以只满足边界条件，而不保持完整代数结构。

这一区分正是“革命发生在深层、经典在表面完整”的代数版本。若要求所有胚层运算都同态投影，就等于取消胚层的大部分独立结构。

### 5.3 补断提供了加法接口，但不能取代整数构造

**定义 5.3（补断加法接口）.** 附件的四同态以

`$$(a,b)+(c,d)=(a+c,b+d)$$$`

及逐分量乘法为特征。[附件 §6] 本报告把这一结构解释为：`$ \pi $` 的左右或实虚纤维可承载 `(0,1)` 型对象。

由点胚得到经典读数对

`$$\pi(\vartheta)=(0,1)$$$`

后，先取由该对生成的自由交换环，再通过差集构造整数商，便得到整数集数：

`$$\mathbb Z_{\mathrm{set}}=\frac{\mathbb N_{\mathrm{set}}\times\mathbb N_{\mathrm{set}}}{\sim},$$$`

其中

`$$(a,b)\sim(c,d)\iff a+d=b+c.$$$`

经典层得到通常整数环。胚层则可记录“正向扭转 `$+1$`”与“负向补断 `$-1$`”的额外结构。原体系关于“零可作除数、射影直线”的说明可以作为整数扭转的几何动机，却不能替代差集等价类的集合论构造。

**命题 5.4（整数生成的接口）.** 设 `$p:\vartheta\to(0_N,1_N)$` 是保持胚内结构的映射。由 `$p$` 诱导的符号差

`$$\mathrm{sgn}(a,b)=\pi(b)-\pi(a)$$$`

在差集商下良定义，并给出投影

`$$\pi_{\mathbb Z}:\mathbb Z_{\mathrm{set}}\longrightarrow\mathbb Z.$$$`

> **证明.** 由差集等价关系的定义，若
>
> `$$(a,b)\sim(c,d),$$$`
>
> 则
>
> `$$\pi(a)+\pi(d)=\pi(b)+\pi(c),$$$`
>
> 从而
>
> `$$\pi(b)-\pi(a)=\pi(d)-\pi(c).$$$`
>
> 因此，差集商到经典整数的映射良定义。□

这里的关键不是把负整数解释为“`$0$` 作除数”，而是先把点胚读数对展开成双向整数群，再研究负向纤维。原体系的 `$1/0=\infty$` 仍只位于微积分与射影层；它不能成为差集构造本身。

## 六、数系逐级扩充中，每一步都必须标明其真正的存在承诺

**从 `$ \mathbb N $` 到 `$ \mathbb C $` 的扩展并非同一算术事实的反复改写，而是不同层次的构造与模型承诺。** 自然数依赖归纳或无穷公理，整数依赖差集商，有理数依赖分式等价类，实数依赖完备性，复数则可由实数对或多项式商构造。

### 6.1 自然数的真正起点是归纳，而不是点胚“既是 0 又是 1”

**定理 6.1（自然数存在）.** 假设 ZF 的无穷公理成立，即存在包含 `$ \varnothing $` 并对 `$x\mapsto x\cup\{x\}$` 封闭的集合；那么由所有归纳集的交集定义 `$ \mathbb N_{\mathrm{set}} $`，可得到满足 Peano 公理的最小归纳集。

南京大学讲义给出该经典结果，并把 `$ \mathbb N $` 定义为归纳集的交集。[3] 在卟学框架中，这只是一条**投影层定理**：经典自然数不必由胚内 `$0=1$` 产生，而是先由空集与后继产生，再要求投影与其相容。

“点胚 `$ \vartheta $` 是第零个集数”与“`$0_N=\varnothing$` 是第一个自然数”并不矛盾。前者属于生成源头或类型原点，后者属于经典可数链的起点。若要进一步令 `$ \pi(\vartheta)=(0_N,1_N)$`，仍须附加结构映射公理；三条原始公理本身不足以证明唯一性。

### 6.2 整数、有理数与实数各依赖不同的构造原则

**定义 6.2（有理数集数）.** 定义

`$$\mathbb Q_{\mathrm{set}}=\frac{\mathbb Z_{\mathrm{set}}\times(\mathbb Z_{\mathrm{set}}\setminus\{0\})}{\approx},$$$`

其中

`$$(a,b)\approx(c,d)\iff ad=cb.$$$`

经典投影按通常分数映射。该构造经典已有，本体系只需增加纤维标记与深度：

`$$\deg([(a,b)])=\max(\deg(a),\deg(b))+1.$$$`

**定义 6.3（实数完备化）.** 设 `$ \mathbb Q_{\mathrm{set}} $` 已装备由绝对值导出的距离。若接受以下两条独立公理之一：

1. **Dedekind 公理：**每个非空有上界子集都有上确界；
2. **Cauchy 公理：**每个柯西序列都可收敛；

则可通过 Dedekind 分割或 Cauchy 等价类构造 `$ \mathbb R_{\mathrm{set}} $`。

有理数虽然稠密，却仍有“缝隙”，其上确界性质可能失效；Dedekind 分割正是通过补齐这些分割来构造实数。[4] **因此，实数完备性不是三条原始公理的逻辑推论，而是扩充层的新公理。** 现有材料不足以在胚层内单独推出完备性，更不能证明“任意点胚极限都存在”。

**定义 6.4（复数集数）.** 定义

`$$\mathbb C_{\mathrm{set}}=\mathbb R_{\mathrm{set}}\times\mathbb R_{\mathrm{set}}.$$$`

乘法按通常复数规则

`$$(a,b)(c,d)=(ac-bd,ad+bc)$$$`

实现。经典层得到代数闭域 `$ \mathbb C $`。

附件以双层莫比乌斯结构解释复数，称实轴与虚轴各为一个莫比乌斯环。[附件 §7.13] 这可以保留为几何图景，但不是由四元数积导出的 `$ \mathbb C $` 集合论定义。从结构复杂度看，`$ \mathbb C $` 是 `$ \mathbb R $` 的二维代数扩张；从生成深度看，实数对的两层构造可以叠加。二者是不同方向的不变量，不能互相替代。

| 扩充层 | 构造原则 | 经典已有 | 卟学新增 | 是否由三条公理推出 |
|---|---|---|---|---|
| `$ \mathbb N $` | 空集＋后继＋归纳/无穷 | 是 | 胚作为第零级读数 | 否，需集数起始公理 |
| `$ \mathbb Z $` | 自然数对之差集商 | 是 | 负向扭转接口 | 否 |
| `$ \mathbb Q $` | 整数对之分式商 | 是 | 四同态截面接口 | 否 |
| `$ \mathbb R $` | Dedekind 或 Cauchy 完备化 | 是 | 完备性纤维化待定义 | **否，独立假设** |
| `$ \mathbb C $` | `$ \mathbb R^2 $` 加复数乘法 | 是 | 双层莫比乌斯解释 | 否 |
| `$ \mathbb H,\mathbb O $` | 2、4、8 维可除代数方向 | 经典对照 | 莫比乌斯扭转展开 | 仅接口，未证明 |

### 6.3 模 8、Clifford 与可除代数应解释扭转，而不能反向定义自然数

**原体系已有：**附件 §10 从四同态的 4 态与 2 极化方向得到 8 个独立方向，并将外代数零基元 `$e_k+e_{k+8}$` 的模 8 周期视为数系扩充的代数来源。[附件 §10.3–§10.5]

**本报告只作弱结论：**

1. 四元数与八元数可以作为实数上的多元数构造研究；
2. 其非交换与非结合性质可以作为“扭转顺序不可随意交换”的模型；
3. 但不能仅凭模 8 周期先验宣称自然数中的素数具有某种分布。

附件明确指出，Hurwitz、Bott 与 Atiyah–Bott–Shapiro 定理只作外部对照，不参与其模 8 证明。[附件 §10] 本报告继承这一边界：Hurwitz 定理确认可除代数维数仅为 1、2、4、8，却不能在卟学内部证明“维数 `$n$` 本身可激活”或“`$n$` 可取任意广义数”。[6]

## 七、生成深度提供了胚层不变量，但必须分层定义，不能伪装成经典自然数

**“数是集数”最自然的贡献不是新素数公式，而是对构造过程本身的测量。** 不同层次的深度定义可以不同；关键是保持类型一致，并明确投影会丢失哪些高阶信息。

### 7.1 有限递归对象的深度可以由递归方案直接定义

**定义 7.1（递归深度）.** 对由有限次构造规则 `$F$` 生成的集数 `$x$`，定义：

- `$ \deg(\vartheta)=0 $`；
- `$ \deg(0_N)=1 $`；
- 若 `$x=S(y)$`，则 `$ \deg(x)=\deg(y)+1 $`；
- 对不交并 `$x+y$`，`$\deg(x+y)=\max(\deg x,\deg y)+1$`；
- 对笛卡尔积 `$x\times y$`，`$\deg(x\times y)=\deg x+\deg y+1$`；
- 对商对象，规定代表元深度在取商后增加 `$1$`。

对于 von Neumann 有限序数，这套定义相容于

`$$\deg(n)=n+1,$$$`

即套括号次数。它并非通用方案：对无限对象、实数完备化或补断截面，必须使用扩展定义。

**命题 7.2（深度单调性）.** 若 `$x,y$` 为自然数型集数，且 `$x<y$`，则

`$$\deg(x)<\deg(y).$$$`

> **证明.** 对 von Neumann 自然数，序数序关系 `$x<y$` 等价于 `$x\in y$`。由 von Neumann 构造，`$y$` 是 `$x$` 的严格超集，因此递归深度严格增加。[2] □

这表明深度在自然数子层中编码了序数信息。但深度不是基数：集合 `{∅,{∅}}` 与其双点结构可以具有不同表示，却在适当同构下具有相同的有限基数。

### 7.2 可构造性、可数性与表示复杂度构成三个不同概念

**定义 7.3（可构造性）.** 给定逻辑语言 `$L$` 与公理系统 `$T$`：

- 若存在 `$T$`-可定义迭代，使得集数 `$x$` 可从 `$ \vartheta $` 经有限或受控超限递归得到，则称 `$x$` **`$T$`-可构造**；
- 若承载集与经典投影均至多可数，则称 `$x$` **可数**；
- 若 `$x$` 由有限次显式规则生成，则称 `$x$` **显式可构造**。

自然数、整数与有理数均可由有限集、笛卡尔积与等价类显式构造，因此是可数的；实数完备化通常产生不可数对象。此处的“通常”来自经典集合论：ZF 足以讨论可数性，实数集的不可数性不依赖卟学新增公理。

**命题 7.4（投影损失）.** 设 `$x$` 为带非平凡 `#` 纤维的胚层对象。即使 `$x$` 显式可构造，也可能出现

`$$\deg(x)>\deg(\pi(x)).$$$`

更一般地，投影可能把高阶内部差异识别为同一个经典读数。

> **证明.** 附件规定 `#` 分量在经典投影下归零。[附件 §2.1] 若两个胚层对象仅在 `#` 纤维上不同，其经典投影可以完全相同，而构造深度不必相同。□

因此，深度首先是胚层复杂度，不是经典数值复杂度。不能仅凭

`$$\pi(x)=\pi(y)$$$`

断言

`$$x=y,$$$`

也不能断言

`$$\deg(x)=\deg(y).$$$`

### 7.3 残留阶数描述的是奇点结构，不能代替集合论深度

**接口定义 7.5（残留阶数）.** 对由补断算子 `$B$` 处理的奇点 `$f$` 与其展开阶 `$n$`，附件定义

`$$r(n;m)=(m-1)-n.$$$`

原体系用它描述 `$x^{-m}$` 型极点的高阶信息，以及何时发生对称抵消。[附件 §5.5] 本报告把它定位为**分析层的局部不变量**，而不是所有集数的递归深度。

二者可以建立联系：把某函数芽的展开系数编码为带类型的集合，其“所需展开阶”“极点阶 `$m$`”与“构造深度”可以形成不等式。但这一对应必须通过显式哥德尔化或系数编码证明，不能预设。

| 不变量 | 对象层级 | 粗略定义 | 在 `$ \pi $` 下是否保持 | 数论用途 |
|---|---|---|---|---|
| 递归深度 `$ \deg $` | 集数 | 构造规则的递归层级 | 一般否 | 复杂度、显式性 |
| 序数 `<` | von Neumann 链 | 集合属于关系 | 是，对经典序数复原 | 归纳、良序 |
| 基数 `$|X|$` | 承载集 | 集合等势类 | 需按承载集定义 | 可数性 |
| 残留阶数 `$r(n;m)$` | 补断分析 | 极点阶数减导数阶 | 仅边界值受控 | 奇点结构 |
| 扭转奇偶 | 莫比乌斯纤维 | 半圈数的奇偶 | 否，可投影归零 | 附件导数符号 |

**命题 7.6（复杂度不增的边界）.** 若 `$x$` 属于 von Neumann 子链，则

`$$\deg(x)=\deg(\pi(x))+1.$$$`

> **证明.** 设 `$ \deg(0_N)=1 $`，并且对经典自然数有
>
> `$$\deg(\pi(n))=\underline n.$$$`
>
> 由递归定义可得
>
> `$$\deg(n)=\underline n+1.$$$`
>
> □

该结果只对已复位到通常自然数的对象成立。对完整胚层对象，不存在理由要求深度在投影后保持。若强制要求“任何 `#` 纤维的深度都可经典观察”，胚层将失去独立信息。

## 八、经典数论通过投影相容接入，但素数理论必须等待独立结构

**现有体系足以定义自然数链、整数与有理数的投影模型，却不足以定义素数。** 投影相容解决的是“卟学是否会破坏经典算术”，不解决“卟学能否产生新的整除理论”。

### 8.1 投影复位表必须同时列出对象、运算与相等

下表中的“兼容”指：经典对象或运算在投影定义域内有预定值，并且卟学构造可通过同构映射复位为经典结构；“独立假设”指三条原始公理尚不能推出相应结论。

| 经典对象/性质 | 卟学载体 | `$ \pi $`-复位 | 相对一致性位置 |
|---|---|---|---|
| `$0,1,2,\dots$` | `$0_N,S(0_N),\dots$` | 是 | von Neumann 子模型 |
| `$+, \cdot, <$` | 并集、笛卡尔积、包含序 | 在经典定义域可要求 | 需逐层同态证明 |
| `$ \mathbb Z,\mathbb Q$` | 差集商、分式商 | 是，经标准同构 | 经典已有 |
| 上确界、极限 | Dedekind/Cauchy 完备化 | 部分，见投影边界 | **独立公理** |
| 极点补断值 | `$B[f]$` 的 `#` 纤维 | 仅边界值，不由 `$ \pi $` 重写 | 原体系核心，待严格化 |
| 素数、`$p\mid n$` | 尚未定义 | 尚无 | **后续接口** |

**命题 8.1（经典数论的可嵌入性）.** 若胚层结构允许一个满足 ZF 子集与函数构造的子类别 `$ \mathcal C $`，并且存在忠实投影 `$ \pi:\mathcal C\to\mathbf{Set} $`，那么经典 Peano 算术可以在 `$ \mathcal C $` 中实现。

> **证明.** 取
>
> `$$0_N=\varnothing,$$$`
>
> `$$S(n)=n\cup\{n\},$$$`
>
> 并将 `$ \mathbb N $` 定义为对 `$S$` 封闭的归纳对象。投影 `$ \pi $` 按恒等映射到 ZF 的 von Neumann 序数。由定理 4.2，所得结构满足 Peano 公理。□

这里的“若”不可删除。把原体系翻译成一个 ZF 模型，需要明确指定：

- 莫比乌斯纤维的集合论载体；
- 扭转 `$T$`；
- 内部等同关系 `$ \simeq_\vartheta $`；
- 投影 `$ \pi $`；
- 补断算子 `$B$`；
- 所有相关等式与残差规则。

原附件尚未给出完整构造，因此该命题只是相对一致性的**充分框架**，不是已证明的相对一致性定理。

### 8.2 与 Robinson、Nelson 和 Colombeau 的比较只能限制在结构意图层面

**Robinson 非标准分析**保持转移原则与超积构造，在标准模型之外添加无穷小与无限大，但不使标准实数中的 `$0$` 获得乘法逆元。**Nelson 的内集论（IST）**在标准 ZF 内重新安排“标准/内”对象的推理规则，也不是让 `$1/0$` 成为标准实数。二者属于保守扩展或新逻辑设置。

**Colombeau 广义函数**则允许包含奇性的光滑函数代数，却以渐近等价类处理乘积不唯一等问题。相关研究指出，Colombeau 广义数在 sharp topology 中的极限行为偏离经典结果，例如经典收敛关系不能逐条照搬。[5] 这意味着“广义对象存在”并不自动意味着“经典算术规则保持不变”。

卟学与这些理论的共同点是试图容纳标准边界情形；差异在于，卟学直接修改 `$1/0$` 的局部代数，并试图通过投影边界避免污染经典算术。现有材料能够支持这一结构意图，却不能据此断言其已经取得与超积构造或非标准模型相同的模型论地位。

### 8.3 素数接口必须同时包含不可约性、整除与投影回经典三个条件

**素数论接入点（占位，不是定义）.** 要在集数框架中研究素数，至少需要以下新结构：

1. **整性层：**在整数集数 `$ \mathbb Z_{\mathrm{set}} $` 上定义加法、乘法与单位元；
2. **可除关系：**定义
   `$$a\mid b\iff\exists q\,(b=aq);$$$`
3. **不可约/素条件：**规定在指定因式分解环境中，何种对象不能被非单位对象分解；
4. **投影相容：**要求经典素数 `$p$` 的胚层提升 `$ \widetilde p $` 满足
   `$$\pi(\widetilde p)=p,$$$`
   并且经典整除关系可通过某种提升关系拉回；
5. **深度赋值：**决定 `$ \deg(p) $` 究竟记录集合递归深度、代表元复杂度，还是新定义的算术复杂度。

在材料尚未确定第 1—5 项时，宣称“素数是奇点”“素数与残留阶数相同”或“模 8 决定素数分布”，都超出了现有依据。正确的纲领性结论是：

> **素数论在 `$ \mathbb Z_{\mathrm{set}} $` 中接入，而不是在 `$ \vartheta $` 内部预先存在。**

点胚可以提供从 `(0,1)` 到双向整数群的生成直觉，也可以为除法奇点提供补断背景；但素性是整环中的乘法不可约性质，至少要求单位、因子与唯一分解环境先被定义。若未来采用代数几何语言，可以把素数理想、概形纤维或 Zariski 拓扑的“断/补”作为候选接口；在此之前，它仍是接口假设。

## 九、九条核心命题足以确立生成纲领，但尚不足以形成完整形式系统

**目前能够成立的，是一组从分层语义出发的“定义—命题—模型假设”链条，而不是无条件公理系统。** 编号保留原论文的宣言风格，但每个结论都明确区分原始公理、本研究构造与尚待验证的模型承诺。

**A1（原始胚公理）** 存在 `$ \vartheta=(\Theta,\simeq_\vartheta,\pi_\vartheta) $`，使得 `$0_\vartheta\simeq_\vartheta1_\vartheta$`，而 `$ \pi_\vartheta(\vartheta)=(0,1) $`。  
**来源：原体系已有。**

**A2（投影边界公理）** `$ \pi $` 不把内部等式视为经典可替换等式；`$ \pi $` 只在经典有定义处复位。  
**来源：原体系已有；本研究重写为类型边界。**

**A3（空集数公理）** 存在经典零集数 `$0_N=\varnothing$`，其深度为 `$1$`。  
**来源：本研究新推，依赖 ZF。**

**A4（并集后继公理）** 对每个集数 `$x$`，定义 `$S(x)=x\cup\{x\}$`；对自然数型集数，`$ \deg(S(x))=\deg(x)+1 $`。  
**来源：经典已有；本研究用于集数生成。**

**A5（归纳公理，卟学改写）** 设 `$P$` 是集数性质。若

- `$P(0_N)$` 成立；
- 对任意经典可读 `$x$`，`$P(x)$` 蕴含 `$P(S(x))$`；

则 `$P$` 对所有 `$n\in\mathbb N_{\mathrm{set}}$` 成立。  
**来源：经典归纳原理；本研究将其限制在经典可读类型。**

**T1（经典自然数复位定理）** 投影 `$ \underline n=\pi(n) $` 构成经典 Peano 链。  
**证明：§4.2。**

**T2（点胚不是后门外泄定理）** 若 `$f$` 保持严格相等，则不能同时把 `$0_\vartheta\simeq_\vartheta1_\vartheta$` 映为 `$0_N=1_N$`。  
**证明：§4.3。**

**T3（整数与有理商结构定理，接口形式）** 由自然数对构造差集商与分式商，可得到 `$ \mathbb Z_{\mathrm{set}},\mathbb Q_{\mathrm{set}} $`；经典投影给出通常 `$ \mathbb Z,\mathbb Q $`。  
**证明：§6.2；经典构造已有。**

**T4（深度不增定理）** 投影一般不能保持 `$ \deg $`；对复位后的 von Neumann 链，存在可计算修正项。  
**证明：§7.2、§7.3。**

**A6（实数完备性独立公理，待接受）** 有理数集数层满足 Dedekind 或 Cauchy 完备性之一。  
**来源：本研究假设。**

**A7（胚层模型假设，待构造）** 存在 ZF 模型 `$M$` 与解释 `$I$`，使得三条原始公理、投影条款与集数生成规则均可在 `$M$` 中实现。  
**来源：本研究假设；原体系宣称“若经典算术一致，则卟学一致”，本报告继承其意图，但不将宣言视为证明。**

A7 是边界而不是缺陷。点胚可以被直观理解为莫比乌斯环、无穷小本体或补断生成元；但要声称它“在 ZF 中存在模型”，就必须给出载体 `$ \Theta $`、内部等同 `$ \simeq_\vartheta $` 与投影 `$ \pi $` 的显式集合论解释。只有完成这一步，无偷渡原则才会从防火墙口号转化为可证明的元定理。

## 十、结论：卟学数论已经形成生成骨架，但一致性、完备性与素数论仍是下一阶段问题

**本报告建立了“革命＋延拓”的最小集合论语义：点胚是原始集数，不是空集；自然数仍从空集经并集式后继递归生成；胚内 `$0\simeq_\vartheta1$` 被禁止穿越投影边界。**

第一，点胚可以作为生成源头，却不能充当 von Neumann 零。令

`$$0_N=\varnothing,\qquad S(x)=x\cup\{x\},$$$`

可以无损恢复经典 Peano 链；点胚只通过投影

`$$\pi(\vartheta)=(0,1)$$$`

提供两叶结构、扭转与奇点补断的解释。原体系关于 `$0=1$` 的“革命性”因而被保存在 `$ \simeq_\vartheta $` 层，而不破坏外部算术。

第二，集数观赋予数系一个天然不变量：**生成深度**。它可定义在自然数、商构造与乘积上，并在 von Neumann 子链中与序数位置相容；但它不是基数，也不能在任意投影下无损保持。实数完备化、补断奇点与莫比乌斯纤维都可能产生“经典投影相同、构造深度不同”的对象。因此，深度不能被伪装成已经解决数论问题的通用概念。

第三，从 `$ \mathbb N $` 到 `$ \mathbb C $` 的扩展路线图已经清楚：自然数依赖归纳或无穷公理，整数与有理数依赖等价类，实数依赖独立的完备性公理，复数及其高维扩展依赖代数结构。原体系的模 8、Clifford 与双层莫比乌斯图景可以作为几何解释，却不能取代这些构造的存在承诺。

第四，本研究主动把素数论留作接口。素性需要乘法环境、单位、整除、不可约性与投影回经典等独立结构；点胚、残留阶数与扭转不足以单凭自身定义它。承认这一点，比把素数直接规定为奇点或模 8 周期更符合材料目前能够支撑的范围。

后续工作的优先级应当是：

1. 给出 `$ \Theta,T,\simeq_\vartheta,\pi,B $` 的显式 ZF 模型；
2. 证明受控泄漏与无偷渡定理；
3. 把残留阶数与补断算子映射为明确的函数空间；
4. 在整数层定义整除、单位与素不可约性；
5. 再研究深度、残留阶数与素性之间是否存在可证联系。

这五项任务依次依赖，不能倒置。尤其是前两项，它们决定原体系究竟只是启发式几何语言，还是具有相对一致性地位的数学理论。

## 数据来源

[1] 用户上传附件：《辩证逻辑下的新数学》（第一手材料），`/data/inputs/辩证逻辑下的新数学.pdf`。

[2] 孙猛，《集合论与图论 第五讲 自然数》，北京大学数学科学学院。  
https://www.math.pku.edu.cn/teachers/sunm/sg/slides/lecture5.pdf

[3] 杨皓，《离散数学讲义：自然数与无穷公理》。  
https://cs.nju.edu.cn/yuanyao/course/dismath/lec4.pdf

[4] 《实数与完备性：Dedekind 分割构造》，腾讯新闻转载科普材料。  
https://news.qq.com/rain/a/20230821A03L6900

[5] Mukhammadiev, A. 等，*Supremum, infimum and hyperlimits in the non-Archimedean ring of Colombeau generalized numbers*，国家科技图书文献中心检索页。  
https://jtys.nstl.gov.cn/paper_detail.html?doi=10.1007/s00605-021-01590-0

[6] 《四元数》（Quaternion），Wikipedia。  
https://en.wikipedia.org/wiki/Quaternion

[7] 《射影决定性公理》，百度百科。  
https://baike.baidu.com/item/%E5%B0%84%E5%BD%B1%E5%86%B3%E5%AE%9A%E6%80%A7%E5%85%AC%E7%90%86/19056492

[8] 《皮亚诺公理》，百度百科。  
https://baike.baidu.com/item/%E7%9A%AE%E4%BA%9A%E8%AF%BA%E7%AE%97%E6%9C%AF/10291113

[9] 《自然数的集合论定义》，搜狗百科。  
https://baike.sogou.com/v178280398.htm